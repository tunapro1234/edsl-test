  
● You're right — they're different experiments. baseline/ is a classic 
  continuous dictator game (give $0–100, naive personas). The Charness-Rabin
  work is a different game and a different method (binary social-preference
  games + persona calibration). Bundling them under 01_dictator/ muddies what
  each is testing. I'll propose a clean split at the end. First, the
  explanation.